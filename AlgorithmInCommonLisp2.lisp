;;;; リストの探索とソートのアルゴリズム
;;;; author: OHASHI Kent

;;; 逐次探索アルゴリズムによりリストlstの指定した要素targetの位置を取得する。
;;; リスト内に該当する要素がない場合、nilを返却する。
(defun sequential-search (lst target)
  ;; TODO: ロジックを実装
  nil)

;;; 二分探索アルゴリズムによりソート済みリストlstの指定した要素targetの位置を取得する。
;;; リスト内に該当する要素がない場合、nilを返却する。
(defun binary-search (lst target)
  ;; TODO: ロジックを実装
  nil)

;;;  バブルソートアルゴリズムによりリストlstを昇順にソートする。
(defun bubble-sort (lst)
  ;; TODO: ロジックを実装
  lst)

;;;  セレクションソートアルゴリズムによりリストlstを昇順にソートする。
(defun selection-sort (lst)
  ;; TODO: ロジックを実装
  lst)

;;;  クイックソートアルゴリズムによりリストlstを昇順にソートする。
(defun quick-sort (lst)
  ;; TODO: ロジックを実装
  lst)
